- classe Game 
- separare Grid in AttackGrid e DefenseGrid ✓
- Player: addShip per i tipi di navi
- DefenseGrid: funzione per controllare se c'è una barca in una posizione ✓
               funzione per ottenere la barca presente in una posizione ✓
               funzione per eliminare una barca affondata dalla griglia
               funzione per aggiornare la posizione delle barche che possono spostarsi
- AttackGrid: funzione per verificare se una posizione è già stata colpita ✓

- Forward declaration tra Grid e Ship:  ✓ (ti prego)
    // HeaderA.h file
    #ifndef H_A_H
    #define H_A_H
    #include "HeaderB.h"
    class A {

    private:
      int b;
    public:
      void function() {
      // using methods of B
      B b;
      b.function();
      }
    };
    #endif

    // HeaderB.h file
    #ifndef H_B_H
    #define H_B_H

    class A;

    class B {
    private:
      int a;
    public:
      void function();
    };
    #endif
